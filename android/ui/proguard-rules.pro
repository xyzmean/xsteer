# Половина на Go приходит готовой библиотекой, а её переходник на Java состоит из имён, по
# которым в неё и обращаются: сжатие выбросило бы их, и отказ был бы не при сборке, а при первом
# подключении.
-keep class xsteer.** { *; }
-keep class go.** { *; }
-dontwarn xsteer.**
-dontwarn go.**
